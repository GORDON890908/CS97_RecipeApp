import React from 'react'

// The Header creates links that can be used to navigate
// between routes.
const Header = () => (
  <header className="col-md-6 offset-md-3 text-center">
    <h1>Recipe App</h1>
  </header>
)

export default Header;